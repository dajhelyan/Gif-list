import GifList from "./components/GifListFunctionalComponent";
import SearchBar from "./components/SearchBar";
function App() {
  // Manage the selected gif logic
  return (
    <div className="App">
      {
        //Todo: Add SearchBar and SelectedGif components
        //Todo: Use class component for GifList instead of functional component
      
      }
      <SearchBar/>
      <GifList/>
    </div>
  );
}

export default App;
